/* Importar la fuente desde Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

/* Estilos generales */
html {
    font-size: 16px; /* Ajuste de la fuente base */
}

body {
    font-family: 'Poppins', Arial, sans-serif; /* Fuente personalizada */
    margin: 0;
    padding: 0;
    background-color: #f4f4f4; /* Gris claro */
    color: #4a4a4a; /* Gris oscuro */
    transition: background-color 0.3s ease, color 0.3s ease; /* Transiciones en fondo y color */
}

/* Menú de navegación */
nav {
    background-color: #a29bfe; /* Lila */
    color: #fff;
    padding: 10px 0;
    text-align: center;
    transition: background-color 0.3s ease, transform 0.3s ease; /* Transiciones suaves */
}

nav:hover {
    background-color: #6c5ce7; /* Cambio de color en hover */
    transform: translateY(-3px); /* Efecto de elevación */
}

nav ul {
    list-style-type: none;
    padding: 0;
}

nav ul li {
    display: inline;
    margin-right: 20px;
}

nav ul li a {
    color: white;
    text-decoration: none;
    font-weight: 600; /* Peso de fuente más grueso */
    padding: 5px 10px;
    border-radius: 5px;
    transition: color 0.3s ease, background-color 0.3s ease; /* Transición en color y fondo */
}

nav ul li a:hover {
    text-decoration: underline;
    color: #dcd6f7; /* Lila más claro */
    background-color: #4a44f5; /* Fondo más oscuro */
}

/* Estilo para el encabezado */
header {
    background-color: #6c5ce7; /* Lila oscuro */
    color: white;
    padding: 40px 0;
    text-align: center;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Sombra suave */
    border-radius: 10px;
    margin-bottom: 20px;
    transition: transform 0.3s ease, background-color 0.3s ease; /* Transición suave */
}

header:hover {
    transform: scale(1.05); /* Efecto de zoom */
    background-color: #5b4dce; /* Cambio de color al pasar el mouse */
}

header img {
    width: 70%; /* Ajusta al 70% del ancho disponible */
    height: auto; /* Mantiene la proporción */
    display: block; /* Para que la imagen se comporte como un bloque */
    margin: 0 auto; /* Centra la imagen */
}

/* Estilo para las secciones */
section {
    font-family: 'Poppins', Arial, sans-serif;
    padding: 25px;
    background-color: #fafafa;
    border-radius: 15px;
    margin: 30px auto;
    max-width: 900px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Sombra suave */
    transition: background-color 0.3s ease, box-shadow 0.3s ease; /* Transición en fondo y sombra */
}

section:hover {
    background-color: #eaeaea;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Sombra más pronunciada */
}

/* Estilo para los artículos */
article {
    background-color: #eaeaea;
    padding: 20px;
    margin-bottom: 15px;
    border-radius: 10px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Transición en transform y sombra */
}

article:hover {
    transform: translateY(-5px); /* Levantar el artículo */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15); /* Sombra más fuerte */
}

/* Estilo para el pie de página */
footer {
    background-color: #a29bfe;
    color: white;
    text-align: center;
    padding: 15px 0;
    margin-top: 30px;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Sombra suave */
    transition: background-color 0.3s ease, transform 0.3s ease; /* Transición suave */
}

footer:hover {
    background-color: #6c5ce7; /* Cambio de color */
    transform: translateY(3px); /* Efecto de elevación */
}

/* Estilo para el formulario */
form {
    width: 100%;
    max-width: 450px;
    margin: 20px auto;
    background-color: #fff;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Transición en transform y sombra */
}

form:hover {
    transform: translateY(-5px); /* Efecto de elevación al pasar */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Sombra más pronunciada */
}

form label {
    font-weight: bold;
    display: block;
    margin-bottom: 10px;
}

form input, form textarea {
    width: 100%;
    padding: 12px;
    margin: 12px 0;
    border: 1px solid #ccc;
    border-radius: 8px;
    box-sizing: border-box;
    transition: border-color 0.3s ease; /* Transición en borde */
}

form input:focus, form textarea:focus {
    border-color: #6c5ce7; /* Borde azul cuando se enfoca */
}

form button {
    background-color: #6c5ce7;
    color: white;
    border: none;
    padding: 15px 20px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    width: 100%;
    transition: background-color 0.3s ease, transform 0.3s ease; /* Transición en fondo y zoom */
}

form button:hover {
    background-color: #4a44f5; /* Lila más oscuro */
    transform: scale(1.05); /* Efecto de zoom */
}

/* Estilo para imágenes */
img {
    width: 100%;
    height: auto;
    border-radius: 10px;
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Transición para imágenes */
}

img:hover {
    transform: scale(1.05); /* Efecto de zoom */
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2); /* Sombra suave */
}

/* Estilo para contenido de la sección con imagen y texto */
section.content {
    display: flex; /* Usa flexbox para alinear la imagen y el texto */
    align-items: center; /* Centra verticalmente */
    padding: 20px;
    max-width: 900px;
    margin: 0 auto;
}

section.content img {
    width: 300px; /* Ajusta el tamaño de la imagen */
    height: auto;
    margin-right: 20px; /* Espacio entre la imagen y el texto */
}

section.content p {
    flex: 1; /* El texto ocupa el espacio restante */
    line-height: 1.6;
}

/* Estilos para la sección del artículo */
.content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.image-right {
    margin-left: 20px; /* Espacio entre el texto y la imagen */
    max-width: 250px;  /* Tamaño máximo de la imagen */
    height: auto;
}
